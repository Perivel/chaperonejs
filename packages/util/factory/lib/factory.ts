import { AbstractFactoryInterface } from './abstract-factory.interfacce';

/**
 * Factory
 * 
 * A Generic Abstract Factory.
 */

export abstract class Factory implements AbstractFactoryInterface {

    constructor() {}
}